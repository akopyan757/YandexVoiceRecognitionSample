<?xml version="1.0" encoding="utf-8"?>
<FrameLayout android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <TextView
        android:id="@+id/tvSpeechResult"
        android:layout_width="wrap_content"
        android:layout_gravity="center"
        android:layout_height="wrap_content"
        android:textSize="@dimen/resultTextSize"
        android:gravity="center"
        android:textColor="@color/black"
        tools:text="Speech" />

    <TextView
        android:id="@+id/tvSpeechStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="@dimen/statusTextSize"
        android:layout_gravity="center_horizontal|top"
        android:textColor="@color/black"
        android:layout_marginTop="@dimen/statusMarginTop"
        tools:text="Status" />

    <Button
        android:id="@+id/btnRecord"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal|bottom"
        android:textSize="@dimen/buttonTextSize"
        android:drawableStart="@drawable/ic_baseline_keyboard_voice_24"
        android:text="@string/record"
        android:layout_gravity="end|bottom"
        android:layout_margin="@dimen/buttonMargin"
        android:contentDescription="@string/todo" />

</FrameLayout>